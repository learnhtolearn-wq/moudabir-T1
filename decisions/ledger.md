# Decision Ledger

Append-only. Every meaningful call gets logged here.
Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---
[2026-08-02] DECISION: Framework = Flutter | REASONING: better offline local-DB perf (Drift/Isar), single codebase for iOS+Android, strong animation support for dashboards | CONTEXT: Intake, stack pick
[2026-08-02] DECISION: Local DB = SQLite (Drift or Isar) encrypted with SQLCipher, key in Keystore/Keychain | REASONING: hard requirement — offline-first, zero cloud, financial data must be encrypted at rest | CONTEXT: Original spec, Phase 0 cadrage
[2026-08-02] DECISION: Auth = PIN + biometric only, no SMS/email 2FA, no server | REASONING: "2FA without cloud" resolved as local PIN+biometric = two local factors; avoids contradicting no-cloud requirement | CONTEXT: Intake, 2FA/notif question
[2026-08-02] DECISION: Notifications = local scheduled only (flutter_local_notifications), no push/backend | REASONING: keeps app 100% offline-first, matches no-cloud constraint | CONTEXT: Intake, 2FA/notif question
[2026-08-02] DECISION: Architecture = Clean Architecture / MVVM + repository pattern | REASONING: isolates local DB, eases testing, keeps door open for optional future sync without rewrite | CONTEXT: Original spec, Phase 0 cadrage
[2026-08-02] DECISION: i18n = FR/AR/EN with RTL from day one | REASONING: hard requirement in original spec | CONTEXT: Original spec
[2026-08-02] DECISION: Scope = build all 9 epics, task by task, in original sprint order (no MVP trimming) | REASONING: explicit user instruction | CONTEXT: Intake, scope question
[2026-08-02] DECISION: Design system pending from separate team, tracked as blocker for UI sprints only | REASONING: data-layer/backend work not blocked by missing mockups | CONTEXT: Intake, design question
[2026-08-02] DECISION: Encryption = sqlite3mc (SQLite3MultipleCiphers) via Dart build hooks, not sqlcipher_flutter_libs | REASONING: sqlcipher_flutter_libs is EOL as of v0.7.0 ("no longer does anything"); sqlite3mc is drift's current documented replacement (verified via drift.simonbinder.eu and pub.dev during Sprint 0 build, Context7 unavailable on this machine — no node/npm installed — so official docs used directly as fallback) | CONTEXT: Sprint 0 build
[2026-08-02] DECISION: State mgmt = Riverpod 2.6.1 (not 3.x) | REASONING: riverpod 3.4.2 pulls a test-package dependency chain that conflicts with drift_dev's analyzer pin; 2.6.1 resolves cleanly | CONTEXT: Sprint 0 build, pub dependency resolution
[2026-08-02] DECISION: Transactions.archived added (schema v1->v2), balance computed on read via aggregate query (not stored/maintained column) | REASONING: matches existing .watch() reactive pattern used by accounts/categories, avoids write-side bookkeeping and drift risk between a stored balance and the transaction ledger; acceptable cost at personal-finance data volumes | CONTEXT: Sprint 2 build, docs/superpowers/specs/2026-08-02-sprint2-transactions-design.md
[2026-08-02] BUGFIX: `_hasCipher` in database.dart checked `PRAGMA cipher_version`, which SQLite3MultipleCiphers does not implement — unrecognized pragmas return an empty result set rather than erroring, so the check always false-failed and threw `StateError` on every encrypted-DB open, silently blocking ALL database writes app-wide (transactions and categories both affected) since Sprint 0. Fixed to `PRAGMA cipher` (per drift's encryption docs), verified working on a physical Android device (Android 11) | REASONING: bug pre-dated Sprint 2 but was only surfaced during Sprint 2's manual end-to-end verification (Task 7), since Sprint 0/1 had no write-path manual test that would have caught a silently-failing write | CONTEXT: Sprint 2 Task 7 manual verification, commit 45f287d
[2026-08-02] BUGFIX/SCHEMA: `Goals` table (defined Sprint 0) was missing an `archived` boolean column despite the CRUD blueprint's soft-delete convention already used by Accounts/Categories/Transactions — added it, bumped schemaVersion 2→3 with an `onUpgrade` `addColumn` step | REASONING: gap only surfaced when Sprint 4 tried to reuse the standard archive() pattern; existing installs need a migration, not a fresh createAll, since Sprint 0-3 users already have data | CONTEXT: Sprint 4 build, goals_provider.dart
[2026-08-02] DECISION: Sprint 4 scope (goal contribute flow) defined inline, no written plan | REASONING: no US-015/US-016 detail exists anywhere in repo beyond the one-line Build Queue entry; built CRUD + a separate "contribute" notifier method (adds to currentAmount, auto-flips achieved at target) as the most direct reading of "savings goals" | CONTEXT: Sprint 4 build, no spec doc precedent (unlike Sprint 2/3 which had docs/superpowers/specs)
[2026-08-02] DECISION: Added Moroccan Darija as 4th supported language, Arabic script, `Locale('ar','MA')` / `ar-MA.json` (not a new language-only locale — kept alongside MSA `ar` since AR covers non-Moroccan Arabic readers) | REASONING: user request; Arabic script chosen over Latin/Arabizi to reuse existing RTL infra and MSA script rendering, least engineering risk; Flutter's GlobalMaterialLocalizations resolves by languageCode only so built-in widget strings (date pickers etc.) fall back to standard Arabic — acceptable since app strings (the vast majority of UI text) render full Darija | CONTEXT: post-Sprint-5, user-requested addition, not in original 9-epic plan
[2026-08-02] BUGFIX: language switch (Settings → Langue) left tab screens (Transactions, Goals, etc.) stuck showing strings in the old locale — AppBar titles, filter chips, search hint all stayed French after switching to Arabic — even though RTL layout mirrored correctly. Root cause: `StatefulShellRoute.indexedStack` caches each bottom-nav tab's page/Navigator; `.tr()` calls inside those cached screens don't reliably re-resolve on locale change even though ambient `Directionality` does. Fixed by giving `MaterialApp.router` a `key: ValueKey(context.locale)` in main.dart, forcing the whole router (and every branch it caches) to fully remount on locale change. User-verified working on physical device (Android 11) after fix | REASONING: guaranteed-correct fix regardless of exact InheritedWidget-propagation mechanism at play; accepted tradeoff is per-tab widget state (scroll position, open forms) resets on language switch, which is fine since it's a deliberate settings action, not incidental | CONTEXT: reported by user while manually verifying Sprint 4, commit pending
